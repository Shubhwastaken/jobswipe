# Test/CI-only dependencies. Kept separate from requirements.txt so the runtime
# image does not ship the test toolchain.
-r requirements.txt
pytest>=8.0
# starlette 0.27 (via fastapi 0.103) drives TestClient with httpx's app= shortcut,
# which httpx 0.28 removed. Pin below 0.28 rather than bump fastapi/starlette and
# risk changing app behaviour.
httpx>=0.27,<0.28
