[pytest]
# Only collect the real suite. Keeps pytest away from ad-hoc scripts in the repo
# root (e.g. test_ml_endpoints.py, which does a live HTTP call at import time).
testpaths = tests
pythonpath = .
